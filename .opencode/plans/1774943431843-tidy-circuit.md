# Chat API Documentation & WebSocket Types Plan

## Summary

Add API descriptions to chat endpoints and create WebSocket event type registry via commonSchemas.

---

## Phase 1: Add Descriptions to Chat Contract

### File: `apps/api/src/app/chat/chat.contract.ts`

Add `summary` and `description` to each endpoint:

```typescript
export const chatContract = {
  createRoom: oc
    .route({
      method: "POST",
      path: "/chat/rooms",
      summary: "Create a new chat room",
    })
    .input(dto.createRoomSchema)
    .output(dto.roomDto),

  getRooms: oc
    .route({
      method: "GET",
      path: "/chat/rooms",
      summary: "List user's chat rooms",
    })
    .input(z.object({}).optional())
    .output(dto.roomListDto),

  getRoom: oc
    .route({
      method: "GET",
      path: "/chat/rooms/{id}",
      summary: "Get room details with members",
    })
    .input(dto.pathIdParam)
    .output(dto.roomWithMembersDto),

  getMembers: oc
    .route({
      method: "GET",
      path: "/chat/rooms/{id}/members",
      summary: "List room members",
    })
    .input(dto.pathIdParam)
    .output(dto.memberListDto),

  getMessages: oc
    .route({
      method: "GET",
      path: "/chat/rooms/{id}/messages",
      summary: "Get room messages (paginated)",
    })
    .input(dto.getMessagesSchema.extend(dto.pathIdParam.shape))
    .output(dto.paginatedMessagesDto),

  createMessage: oc
    .route({
      method: "POST",
      path: "/chat/rooms/{id}/messages",
      summary: "Send a message",
    })
    .input(dto.createMessageSchema.extend(dto.pathIdParam.shape))
    .output(dto.messageDto),

  inviteMember: oc
    .route({
      method: "POST",
      path: "/chat/rooms/{id}/invite",
      summary: "Invite user to room",
    })
    .input(dto.inviteMemberSchema.extend(dto.pathIdParam.shape))
    .output(dto.invitationDto),

  getInvitations: oc
    .route({
      method: "GET",
      path: "/chat/invitations",
      summary: "List pending invitations",
    })
    .input(z.object({}).optional())
    .output(dto.invitationListDto),

  acceptInvitation: oc
    .route({
      method: "POST",
      path: "/chat/invitations/{id}/accept",
      summary: "Accept invitation",
    })
    .input(dto.pathIdParam)
    .output(dto.invitationDto),

  declineInvitation: oc
    .route({
      method: "POST",
      path: "/chat/invitations/{id}/decline",
      summary: "Decline invitation",
    })
    .input(dto.pathIdParam)
    .output(dto.invitationDto),

  leaveRoom: oc
    .route({
      method: "POST",
      path: "/chat/rooms/{id}/leave",
      summary: "Leave a room",
    })
    .input(dto.pathIdParam)
    .output(dto.successDto),

  deleteRoom: oc
    .route({
      method: "DELETE",
      path: "/chat/rooms/{id}",
      summary: "Delete room (owner only)",
    })
    .input(dto.pathIdParam)
    .output(dto.successDto),
};
```

---

## Phase 2: Create WebSocket Events Schema File

### New File: `apps/api/src/app/chat/chat-events.ts`

```typescript
import { z } from "zod";

// Event Name Enums (for type-safe event naming)
export const clientToServerEventNameEnum = z.enum([
  "join_room",
  "leave_room",
  "send_message",
  "typing_start",
  "typing_stop",
  "heartbeat",
]);

export const serverToClientEventNameEnum = z.enum([
  "message:new",
  "user:typing",
  "user:presence",
  "member:joined",
  "member:left",
  "room:deleted",
]);

// Client → Server (Input Events)
export const wsJoinRoomInputSchema = z.object({
  roomId: z.string().uuid(),
});

export const wsLeaveRoomInputSchema = z.object({
  roomId: z.string().uuid(),
});

export const wsSendMessageInputSchema = z.object({
  roomId: z.string().uuid(),
  content: z.string().min(1).max(5000),
});

export const wsTypingInputSchema = z.object({
  roomId: z.string().uuid(),
});

// Server → Client (Output Events)
export const wsMessageNewOutputSchema = z.object({
  roomId: z.string(),
  senderId: z.string(),
  senderName: z.string(),
  content: z.string(),
});

export const wsUserPresenceOutputSchema = z.object({
  userId: z.string(),
  userName: z.string().optional(),
  roomId: z.string(),
  status: z.enum(["online", "offline"]),
});

export const wsUserTypingOutputSchema = z.object({
  userId: z.string(),
  userName: z.string().optional(),
  roomId: z.string(),
  isTyping: z.boolean(),
});

export const wsErrorOutputSchema = z.object({
  message: z.string(),
});

// Types
export type ClientToServerEventName = z.infer<
  typeof clientToServerEventNameEnum
>;
export type ServerToClientEventName = z.infer<
  typeof serverToClientEventNameEnum
>;
export type WsJoinRoomInput = z.infer<typeof wsJoinRoomInputSchema>;
export type WsLeaveRoomInput = z.infer<typeof wsLeaveRoomInputSchema>;
export type WsSendMessageInput = z.infer<typeof wsSendMessageInputSchema>;
export type WsTypingInput = z.infer<typeof wsTypingInputSchema>;
export type WsMessageNewOutput = z.infer<typeof wsMessageNewOutputSchema>;
export type WsUserPresenceOutput = z.infer<typeof wsUserPresenceOutputSchema>;
export type WsUserTypingOutput = z.infer<typeof wsUserTypingOutputSchema>;
export type WsErrorOutput = z.infer<typeof wsErrorOutputSchema>;
```

---

## Phase 3: Update commonSchemas in OpenAPI

### File: `apps/api/src/app.contract.ts`

```typescript
import { chatContract } from "./app/chat/chat.contract.js";
import * as chatEvents from "./app/chat/chat-events.js";

export const router = {
  chat: chatContract,
};

export const commonSchemas = {
  // Event Name Enums
  ClientToServerEventName: { schema: chatEvents.clientToServerEventNameEnum },
  ServerToClientEventName: { schema: chatEvents.serverToClientEventNameEnum },
  // WebSocket Input Events
  wsJoinRoomInput: { schema: chatEvents.wsJoinRoomInputSchema },
  wsLeaveRoomInput: { schema: chatEvents.wsLeaveRoomInputSchema },
  wsSendMessageInput: { schema: chatEvents.wsSendMessageInputSchema },
  wsTypingInput: { schema: chatEvents.wsTypingInputSchema },
  // WebSocket Output Events
  wsMessageNewOutput: { schema: chatEvents.wsMessageNewOutputSchema },
  wsUserPresenceOutput: { schema: chatEvents.wsUserPresenceOutputSchema },
  wsUserTypingOutput: { schema: chatEvents.wsUserTypingOutputSchema },
  wsErrorOutput: { schema: chatEvents.wsErrorOutputSchema },
};
```

---

## Phase 4: Update openapi.ts

### File: `apps/api/src/lib/openapi.ts`

Import commonSchemas and pass to generator:

```typescript
import { commonSchemas } from "#src/app.contract.js";

export const genOpenapiDocs = async () => {
  // ...
  const spec = await generator.generate(router, {
    // ...
    commonSchemas,
  });
};
```

---

## Phase 5: Update Gateway to Use New Schema Types

### File: `apps/api/src/app/chat/chat.gateway.ts`

Update imports to use `chat-events.ts`:

```typescript
import {
  wsJoinRoomInputSchema,
  wsLeaveRoomInputSchema,
  wsSendMessageInputSchema,
  wsTypingInputSchema,
  type WsJoinRoomInput,
  type WsLeaveRoomInput,
  type WsSendMessageInput,
  type WsTypingInput,
} from "./chat-events.js";
```

---

## Files to Create/Modify

| File                                     | Action                                |
| ---------------------------------------- | ------------------------------------- |
| `apps/api/src/app/chat/chat.contract.ts` | Modify - add summaries/descriptions   |
| `apps/api/src/app/chat/chat-events.ts`   | Create - WebSocket event schemas      |
| `apps/api/src/app/chat/chat.gateway.ts`  | Modify - use new schema imports       |
| `apps/api/src/app.contract.ts`           | Modify - add commonSchemas            |
| `apps/api/src/lib/openapi.ts`            | Modify - import and use commonSchemas |

---

## Verification

```bash
# Run typecheck
pnpm typecheck

# Build SDK to regenerate types
cd packages/sdk && pnpm build

# View OpenAPI docs at /api/docs (scalar) after running dev
```
