{
  "name": "converge",
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "app": "pnpm --filter app",
    "api": "pnpm --filter api",
    "email": "pnpm --filter email",
    "ui": "pnpm --filter design-system",
    "db": "pnpm --filter database",
    "sdk": "pnpm --filter sdk",
    "auth": "pnpm --filter auth",
    "analytics": "pnpm --filter analytics",
    "build": "turbo build",
    "dev": "turbo dev",
    "dx": "turbo dx",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "lint:type-aware": "oxlint --type-aware",
    "format": "turbo format",
    "format:check": "turbo format:check",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "analyze": "turbo analyze",
    "bump-deps": "pnpx npm-check-updates --deep -u -x recharts && pnpm install",
    "changeset": "changeset",
    "changeset:version": "changeset version",
    "changeset:publish": "changeset tag",
    "prepare": "husky"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.8",
    "@repo/typescript-config": "workspace:*",
    "@turbo/gen": "^2.6.3",
    "@types/node": "^24.10.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "oxfmt": "latest",
    "oxlint": "latest",
    "oxlint-tsgolint": "latest",
    "tsup": "^8.5.1",
    "turbo": "^2.8.13",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.24.0",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@nestjs/core",
      "@prisma/engines",
      "@sentry/cli",
      "@swc/core",
      "bufferutil",
      "core-js",
      "core-js-pure",
      "esbuild",
      "msgpackr-extract",
      "prisma",
      "sharp"
    ]
  }
}
